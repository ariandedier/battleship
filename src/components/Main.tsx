import styled from 'styled-components'


const Main = () => {
  return (
    <MainWrapper>
      
    </MainWrapper>
  )
}

const MainWrapper = styled.main`
  padding: ${({ theme }) => theme.padding.lg};
`

export default Main
